# dig

The dig (Domain Information Groper) command is used for querying DNS (Domain Name System) name servers, check which IP a domain resolves to, and diagnose DNS problems

## Basic Usage

dig google.com

Or

dig +short google.com
- Returns just the IPs
- Works even if browser can't resolve names